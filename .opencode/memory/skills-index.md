# stable-workflow v2 (2026-09-21) — local-first, global on-demand. Loaded as instructions every turn, keep tiny.
# Rule: build local first, prove on 1 real task, then promote. Never load full memory; read index only, fetch detail via skill-keeper when trigger matches.
# AUTO-RULE: after every edit/write/bash that changes files, auto-call supervisor via Task (PASS or FIX, max 1 re-check); if Task subagents unavailable, manual self-check with same bar (logic errors only). Skip for read-only answers.

- 0-ALWAYS | deliverable-first ("Docs, website, or other?"), one primary action + one box, TL;DR + disclosure, cut extra words, approval before combining projects | style: most efficient + beautiful (NOT simplest-bare; beauty-creative judgment mandatory every UI, Apple polish by default, smooth auto-chain flow, fewest steps that still ship polish)
- 1-DEFINE (global, light) | vague ask → interview-me (1 Q at a time); rough concept → idea-refine; multi-capability feature → spec-driven-development (capability map first); no quality bar → constraint-driven-development → CONSTRAINTS.md
- 2-PLAN | local planning-discipline WINS: plan mode only when 3+ files or contract/schema/auth change or unfamiliar code; format Context/Questions(2-4)/Steps(Goal,Scope,Out-of-scope,Done-when)/Risks; save to PLANS.md, implement after approval | borrow from planning-and-task-breakdown: dependency graph + acceptance criteria per task; SKIP global 4-gate ceremony + tasks/plan.md for small tasks
- 3-BUILD-UI | local WINS by default: website-build (static Build→Check→Deliver, 1920px + 390px, bento one-dominates) + beauty-creative (mandatory taste gate: symmetry/balance, proportion, fluency, naturalness, context-fit; Apple tokens by default) + static-motion (transform/opacity 150-300ms, reduced-motion off) | site-mimic only on mimic/clone ask (observe→scaffold→structure→glass→motion, original assets only, evidence to docs/evidence/) | global frontend-ui-engineering ONLY on explicit React/app/state ask; api-and-interface-design only on contract-first API ask
- 3-BUILD-VISUAL | no conflict, local owns: diagram-generate (matplotlib/PIL/Mermaid/SVG, Temp first, confirm before docs/) + textbook-illustration (max 4 numbered panels, caption = what to notice) | briefing-style = script ref (brief.py render_daily_md/render_monday_md), not a full skill
- 3-BUILD-CODE | incremental-implementation (thin slices ~100 lines, verify each) + source-driven-development (docs-cited) on unfamiliar lib; doubt-driven-development only on high-stakes/auth/security/migration | context-engineering on session degrade
- 3-PARALLEL (Orca worktrees, on-demand) | fan-out ONLY when: (a) independent streams (no shared files, e.g. DOC1 content vs VN posters vs tooling) or (b) design variants to compare (2-3 options, user merges winner) | rules: one agent owns one area (never two hands in build.py), each agent in own worktree, supervisor = merge gate, small/sequential tasks stay single-agent (coordination > work)
- 4-VERIFY | browser-skill WINS (bsk logged-in Chromium, observe before act, fresh @eN refs, session stop); browser-testing-with-devtools DORMANT unless chrome-devtools MCP configured | test-driven-development ONLY for logic/bugfix/behavior change (red-green-refactor with repo's own runner); debugging-and-error-recovery on break (reproduce→guard)
- 5-REVIEW+SHIP | supervisor = mandatory gate; code-review-and-quality 5-axis + code-simplification + security-and-hardening + performance-optimization ON-DEMAND (before merge/publish/slow); git-workflow-and-versioning (atomic commits, brief auto-push stays) + shipping-and-launch + ci-cd-and-automation + deprecation-and-migration + documentation-and-adrs (why-docs) + observability only when asked; deliver: ask "auto-update or manual OK?" once (Pages push/cron), never publish tokens
- META | skill-improvement-development owns new-skill/skill-fix (local-first, prove on 1 task, then promote); skill-keeper owns memory writes on DONE only
- GUARDS | guidebook/class-guide → guidebook/*.md only, never academic-paper | English lesson/practice → language-tutor only | graphify: this project stays graph-free (<30 files logic, no graphify-out/)
